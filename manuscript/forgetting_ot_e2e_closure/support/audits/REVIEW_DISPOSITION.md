# New-manuscript review: evidence checks and disposition

Current integration date: 2026-09-09; manuscript base `86d09f1`. Only the English manuscript is edited. Evidence paths below are relative to `C:/Users/ASUS/Documents/ai训练遗忘性研究`; manuscript paths are relative to the English manuscript. Frozen measurements retain their original precision. The final section gives current experiment status and claim mapping. Verification is agent file inspection and CPU reconciliation; human author approval is pending.

## Historical writing-only review (base `761d1d9`)

Items 1--8 and their delivery record below describe the preceding revision. Their statements that prediction and matched PyKeOps work are open are superseded by the 2026-09-09 integration at the end of this document.

## 1. Prior execution systems and OT adaptation

**Premise:** accepted: completion-aware execution predates this work. MLSys 2020 auto-batching advances members by program position; OSDI 2022 Orca schedules at iteration boundaries; Ginkgo has independent per-system stopping; jaxipm replaces completed problems. The alternating-scaling identity is standard Sinkhorn structure, not a newly discovered theorem. Sources (external originals): [Radul et al., Sections 2-3](https://proceedings.mlsys.org/paper_files/paper/2020/file/39945d578f616735572174bf5e8f155d-Paper.pdf), [Orca, Sections 1 and 3](https://www.usenix.org/system/files/osdi22-yu.pdf), [Ginkgo batched CG: stopping rule](https://gko-project.org/user-guide/concepts/batched/cg.html), [Kashi et al., Sections 2-4](https://publikationen.bibliothek.kit.edu/1000158257/156817569), [jaxipm, introduction and batched method](https://arxiv.org/html/2606.26341v1).

**Impact/scope:** novelty framing in introduction, related work, and method. **Action:** explicitly acknowledge the execution lineage, identify the OT adaptation as one-marginal screening followed by full verification and aligned matrix-free state compaction, and distinguish draining a finite batch from replacement. MLSys and OSDI are named accurately; jaxipm remains a preprint, not an invented conference publication. The component paragraph states that the screen saves detection work while compaction supplies the larger timing effect. Source for component attribution: current manuscript Table 5 and `knowledge/audits/SOURCE_BOUND_REVIEW_RECHECK_FINAL_20260905_ZH.md`, W5 row. No claim that verification cost is halved is added.

## 2. Numerical precision and uncertainty

**Premise:** accept excessive displayed precision; a narrow interval alone does not establish invalid measurements. **Impact/scope:** readability and statistical interpretation throughout the PDF. **Action:** performance values use three significant digits; exact counts, configured tolerances, seed IDs, and software versions retain their meaning. Narrow timing intervals are expressed as percent change in the timing ratio, `100(S-1)`, in Appendix A.1, with the fixed-input statistical unit stated once. No interval is fabricated, silently widened, or used as workload-generalization uncertainty.

Sources (frozen analysis): `archive/inspections/C80_LM_20260828/analysis_extracted/c80_lm_analysis_793c08c_20260828T1825Z/analysis.json`, primary LM/PC; `knowledge/audits/DRAINSINKHORN_ACCURACY_CONTRACT_AND_SPEEDUP_AUDIT_20260828_ZH.md`, consumer-subtracted interval; manuscript `support/figure_materials/PACKER19_STOPPING_METRICS.csv`, simultaneous-completion row. Table 4 now uses mean and sample SD across three seeds, recomputed from `studies/campaigns/C63_semantic_latent_OTFM_feature_training/analysis.json`, `summary.raw_rows[*].methods[*].fixed_update_evaluation_by_nfe`. All 18 mean/SD pairs were checked against the frozen `quality_by_nfe` values. The group ratios are rounded from `knowledge/audits/M3_GROUPING_IMPLEMENTATION_AUDIT_20260827_ZH.json`, `condition_depth_metrics`, avoiding double rounding of homogeneous and random-layout results.

## 3. Performance prediction

**Premise:** partly accepted. Work-count identities and a cost accounting equation alone do not validate application prediction. The assertion that all required matching inputs already exist was not confirmed. **Impact/scope:** contribution strength and model validation. **Action completed:** distinguish work identities, calibrated cost approximation, and conditional prediction; give the conditional speedup estimator and its independent-calibration/held-out requirements. The second contribution now emphasizes empirical A100 width-cost characterization.

**Still open:** an application prediction-versus-measurement result. Sources (measurement/configuration): `studies/local_results/A100_WIDTH_COST_ENGINEERING_20260907T205000Z_73aec294/n16384/width_cost_curve.json`, arguments (`dim=500`, `block_n=64`, `cost_scale=1`); the same study's `REPORT_ZH.md`, Sections 4-5 (Metro component calibration has `dim=15`, `block_n=128`, different cost scale; compaction instability); `studies/campaigns/C80_LM_logical_mask_attribution/manifest.json`, workload (`dim=64`). These cannot be combined into a numerical Packer19 prediction. A curve for another backend also needs its own screening, verification, compaction, and tail-path costs. No predicted speedup or prediction accuracy is invented.

## 4. PyKeOps checking-frequency confound

**Premise:** accepted for compaction-only attribution. **Impact/scope:** the PyKeOps configuration comparison, not the matched Triton LM/DC experiment. **Action completed:** preserve the reported configuration results, state the checking-frequency factor, and state that the runner supports every-update compaction. This is not a PyKeOps mechanism restriction. Sources: `studies/reports/IMAGENET32_KEOPS_PACKED_RESULTS_20260808.md`, comparison/decomposition tables; implementation `studies/code/imagenet32_keops_packed_eot.py`, lines 390-421, 505-535, and 815-820 (`audit_interval`, effective checking interval).

**Still open:** formally measured static/mask/compaction results at checking interval one. The source-bound ledger and opened report do not contain this matched cell. A result must rerun all compared paths under the current source and a common interval, rather than attach a new active run to an old static denominator. Required runner configuration: existing input cache and original support/window/seed settings, `--audit-interval 1 --policies static_padded masked_batch active_fixed`; balance method orders and retain the common final verifier. Preserve the same-backend baseline, first-passage/release traces, physical widths, raw timings, environment, source commit, and consumer/output checks. This turn did not run this experiment.

## 5. Method figure

**Premise:** accepted. **Impact/scope:** method comprehension. **Action:** Figure 1 now pairs the execution loop with a real active-width trace. The old work-count-only figure is removed from the active inventory and recoverable from Git. Sources: method equations and implementation state-selection contract in `sections/3_method.tex`; raw `studies/raw_results/server_runs/20260824_c35_imagenet32_width_sweep_04d0f5c/w4/local-active/node0/results/tasks/imagenet32-imagenet32-pca500-shard000-affinity0003-active/result.json`, window seed 2026082400/start 0. Its completion counts `(130,75,90,70)` give `sum(n_t)=365`, hypothetical fixed work `4*130=520`, and removed work `155`. This is an actual recorded example, not a synthetic timing claim.

## 6. Initial-width sensitivity

**Premise:** partly accepted: the earlier manuscript omitted the existing W sweep; wider batches do not guarantee monotonically greater speedup for arbitrary workloads. **Impact/scope:** operating regime. **Action completed:** Figure 4 shows W=1,2,4,8,16, with all 40 windows per width in the padding panel and the original complete-configuration timing comparison in the runtime panel. Sources: `studies/raw_results/server_runs/20260824_c35_imagenet32_width_sweep_04d0f5c/analysis/width_sweep.csv` and its `w*/local-active/node*/results/tasks/*/result.json`. All 200 recorded windows satisfy `candidate_slots == sum(iterations) == sum(active_width_trace)`. The sequential/projected-carry versus shared-initialization baseline identity is explicit; the curve is not labeled compaction-only.

**W=32/64 budget estimate, not measured:** at W16, the recorded eight-worker critical paths are 102.013 s for the baseline and 55.2872 s for active execution (same CSV, W16 row). A linear-in-W planning extrapolation for both added widths gives `(2+4)*(102.013+55.2872)=944 s`, about 16 minutes elapsed or 2.10 GPU-hours at eight workers, excluding setup, compilation, retries, and any additional repetitions. This assumes runtime scales linearly; it is a budget scenario, not a runtime prediction or a guarantee. Both new widths remain unmeasured in this revision. The existing five widths are reused first.

## 7. Statistical units and grouping

**Premise:** accept that execution orders are timing repetitions for fixed inputs. Grouping the same 32 problems changes assignment, not independent dataset coverage. **Impact/scope:** application-result interpretation. **Action:** Table 2 now identifies one workload/three orders; Section 5.2 immediately points to the grouping intervention and describes its fixed pool. Sources: `studies/campaigns/C82_MetroPT3_static_active_corrected/manifest.json` and `analysis.json`; `studies/campaigns/EXP-M3_MetroPT_failure4_depth_heterogeneity/README.md`, formal design. No new generalization sample size is claimed.

## 8. Setup and numerical safety

**Premise:** accepted for primary Packer19 tolerance and safety organization. **Impact/scope:** experimental setup and result interpretation. **Action:** Table 1 uses the component tolerance 1e-3 and directs readers to the sweep. Section 5.6 brings together emitted-plan residuals, screening replay, LM/DC output parity, training quality, and the separate precision stress results; its guarded configuration remains explicitly separate from timed applications. Sources: `studies/campaigns/C80_LM_logical_mask_attribution/manifest.json`, `control_contract` and `formal_result`; `knowledge/audits/SOURCE_BOUND_REVIEW_RECHECK_FINAL_20260905_ZH.md`, W5; existing guarded-stress evidence in ledger C10 and the base manuscript Appendix A.5. No new safety guarantee is inferred from zero observed errors.

## Reproduction and delivery status

Final local validation: recomputing from the frozen evidence exactly reproduces `review_data.json`, including all 18 quality mean/SD pairs and 200 width-sweep windows. The rebuilt PDF has 13 pages, Tables 1--9 and Figures 1--4 in order, no rendered campaign codes, and no overfull-box or undefined-reference warnings. Rendered method, result-table, runtime-figure, and appendix pages were inspected for readability. These checks validate the manuscript and reanalysis, not new GPU performance results.

`python support/figure_materials/reviewer_reanalysis.py --evidence-root <evidence-repository>` recomputes the numeric packet from frozen records. `review_data.json` contains the derived values and source identities; `render_figures.py` renders the W figure and stopping labels. This is local reanalysis, not new A100 performance evidence. Outstanding prediction and PyKeOps matched-cell work above must not be marked complete by a later editor merely because the prose was revised.

## Current evidence integration: 2026-09-09

The completed server experiments are evidence-repository commits `96c8cae7` (competitive controls) and `81f9a2b2` (matched PyKeOps). This manuscript task performs CPU reanalysis and writing only. It creates no new GPU observations. The root ledger's Section 13, the campaign protocols, raw packets, frozen analyses, and independent CPU audit were opened before editing. The earlier scheduler report and its code/raw record were also inspected. Existing publications and their priority claims are unchanged in this revision.

### Premise, impact, scope, and disposition

| Issue | Evidence check and impact | Disposition and manuscript locations |
|---|---|---|
| Full-width masking is not a compute-skipping competitor | Accepted for attribution. `studies/code/benchmark_flash_compaction_scheduler.py`, `GraphWorkspace._host_block` and `move_from`, still updates the whole physical workspace and moves state when shrinking. It supplies scheduling controls, not active-index/entry-skip kernels. New implementation: `algorithm/src/sinkhorn_ops/_flash_packed_triton.py`, indexed/entry-skip branches; `studies/code/run_compaction_competitive_controls.py`, `Execution.update` and pointer gates. | Abstract, Introduction, Method, Sections 5.1/5.4/5.7, Discussion and Conclusion now distinguish completed-compute removal from state movement. Historical Packer19 LM/DC is retained with its actual baseline; new MetroPT-3 no-copy comparisons have differences below 0.1%. |
| Quantitative performance-model evidence | Accepted, with correction to the old gap description: real-input block/movement calibration already existed in the scheduler probe (`calibration` in its raw JSON; runner `calibrate_costs`). The old phase costs are directly measured, not a held-out total-time prediction. New independent cost reconstruction validates observed traces on separate timing runs and transfers first-group calibration to disjoint target groups. | Section 4 gives the evaluated estimator and event-cost rules; Section 5.7/Table 7 gives measured times and signed error ranges. Positive errors of 0.463--1.08% cover three primary methods/four repeats on transferred target groups. Abstract and contribution text explicitly condition on observed completion. |
| Input representativeness | Partly accepted as a remaining scope limit. Old scheduler `data.npz_sha256` equals new `provenance.data_sha256`; four W4 groups and the overlapping W8 group reuse the same 16 target IDs/shared source. Sources: old/new raw packets below, new `args`, `depths`, and independent `AUDIT.md`. | Sections 5.7 and A.7 state reuse and grouping; timing repetitions are kept separate from input counts. The 32-problem grouping intervention remains a separate experiment. No new-dataset or independent-window coverage is inferred. |
| Actual-path numerical precision | New saved-output replay addresses the measured MetroPT-3 path. `raw/*.pt` retain first-timed potentials; `raw/*.json:fp64` contains 96 method--problem checks, maximum 0.0009476390364203683, covering 16 distinct targets. Source: competitive `AUDIT.md`, saved-output section, and runner's blocked direct-dot replay. The historical pressure-test summary gives disagreement totals only. | Sections 3.2/3.3 distinguish backend residual decisions from reference evaluation; Sections 5.1/5.6 and A.7 specify FP32/TF32-off, equal internal screen/verify thresholds 0.00095, external 0.001, and untimed FP64 replay. The independent guarded stress configuration remains separate. |
| Matched PyKeOps interval | Completed measured cell: every-update static/mask/active, two sizes, three inputs and three orders. Source: `EXP_KeOps_Check_Interval_20260908/analysis/summary.json`, `per_arm.csv`, `per_seed.csv`, raw formal packets and README timing contract. | Table 3 adds a separately labeled block; Sections 5.2/A.3 state aggregation, coupling-preparation endpoint and the retained fallback. Ratios 0.999 and 1.02 are means of input-level median paired static/active ratios; no historical denominator is reused. |

### Source-bound claim and display inventory

Evidence locations below are machine-linked in `support/figure_materials/competitive_data.json` with SHA-256 and computed values. The script records agent reconciliation, not human approval.

- **Compute-skipping times, paired effects and model errors:** `studies/campaigns/EXP_Compaction_Competitive_Controls_20260908/raw/metro_n16384_w4_offset{0,4,8,12}.json` and `metro_n16384_w8_offset0.json`; matching `_analysis.json` for offset0, `_heldout_analysis.json` for transferred W4 groups. Exact keys: `runs[*].solver_seconds`, `trace`, `controls`, `width_calibration`, `calibration`, and analysis `primary_contrasts` / `model.methods[*]`. Formula: per-repeat control/DC then median; model error `100*(estimate/actual-1)`, range over the three primary methods and four repeats. Manuscript: abstract, introduction, Section 4 estimator, Section 5.7 and Table 7. Legacy LM remains secondary because its allocation/restore path differs.
- **Table 7 contract:** editable monochrome LaTeX lookup table; three absolute-time columns show the near-equal costs without magnifying tiny ratios, with a separate error-range column and same-input/transfer labels. Generated by `competitive_reanalysis.py`; source values remain full precision in `competitive_data.json`, display uses three significant digits. This table supports implementation attribution and quantitative conditional reconstruction. No invented uncertainty or equivalence test is added.
- **Table 3 added block:** `studies/campaigns/EXP_KeOps_Check_Interval_20260908/analysis/summary.json:across_seeds`, `seed_medians`, and `analysis/per_arm.csv`. The script independently reconciles means of seed medians. Existing top/middle blocks retain their own sources, ratios, and timing contracts; the new block is not normalized to them.
- **Old scheduler comparison:** `studies/local_results/FLASH_COMPACTION_SCHEDULER_METROPT3_20260908_A100/raw_results.json:summary`, `calibration`, `runs`, and `REPORT_ZH.md` Sections 2--4. Its W16 exact-host/graph/bucket medians round to 8.43/8.42/9.24 s. New controls use W4/W8 and their own buffers/tail path, so no old/new timing ratio is formed. Manuscript: Section 5.7 final paragraph and Appendix A.7.
- **Actual-output checks:** the five competitive raw packets' `fp64` records plus their twenty `.pt` siblings; independent `AUDIT.md`, saved-output section. Manuscript: Section 5.6, with counts and backend-vs-reference distinction. No actual-output Packer19 FP64 conclusion is added.

### Remaining evidence boundaries

The competitive Packer19 comparison remains pending because prepared input coordinates were not located; partial dual-only state cannot replace them (competitive campaign README, Remaining coverage). Broader real-input coverage and a matched lower-kernel performance comparison to upstream FlashSinkhorn are not supplied by the new MetroPT-3 matrix. These are explicitly unclosed, rather than covered by repetition counts or the presence of the FlashSinkhorn citation. Manuscript claims are restricted to the measured implementation and inputs.

### Validation

**Control provenance clarification (2026-09-09):** Sections 2.2/2.3 and 5.1 now identify logical masking as our experimental control implemented within the OT backend. Indexed execution and entry skipping are also our measured adaptations. Related systems motivate execution ideas; their published solvers are not the implementations timed in these comparisons. Source (code): `algorithm/src/sinkhorn_ops/flash_packed.py`, `_freeze_retired_potentials` and the `retirement_mode='logical_mask'` branch; `studies/code/run_compaction_competitive_controls.py`, `Execution.update`. Source (frozen design): `studies/campaigns/C80_LM_logical_mask_attribution/manifest.json`, `primary_contrast` and `control_contract.backend`. The user's provenance clarification is consistent with these local implementation records. No universal claim that logical masking has never appeared in any OT work is added, and Ginkgo's actual compute stopping is kept distinct from our full-width freeze control. Numerical results and comparison definitions are unchanged. Scientific-writing supplies the source binding; anti-defensive-writing keeps this as a positive authorship statement.

`competitive_reanalysis.py --evidence-root <evidence-repository> --check` checks equality of all five rendered-table analyses with recomputation from raw packets, the input-identity reuse, all FP64 record gates/counts, and PyKeOps summary aggregation. Table 5 stays in Section 5.4; Table 6 stays in Section 5.5; Table 7 is new in Section 5.7. Current build and rendered-page review are recorded in `VISUAL_REVIEW.md`.
