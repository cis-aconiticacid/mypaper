\section{Hardware-Aware Performance Model}
\label{sec:cost-hypothesis}

The model combines per-problem completion depths with measured update time at
each active batch width. Completion depths determine how many updates can be
removed; width-dependent kernel time determines the runtime reduction. This
follows the standard use of accelerator performance models to relate workload
shape and hardware execution cost \citep{yu2021habitat,nvidia2022gpuperformance}.

\subsection{Counting removed updates}

For problem $t$, let $n_t$ be the batched-iteration number of the first
configured verification round at which the problem passes the two-marginal
verifier, and let
\begin{equation}
A_k=\bigl|\{t:n_t\geq k\}\bigr|
\label{eq:active-width}
\end{equation}
be the active batch width before packed update $k$. Verification
supplies both quantities.

\begin{proposition}[Update-count identity]
For a finite window of $W$ problems,
\begin{equation}
S_{\mathrm{active}}
=\sum_{k=1}^{n_{\max}}A_k
=\sum_{t=1}^{W}n_t,
\qquad
S_{\mathrm{static}}=Wn_{\max}.
\label{eq:update-count-identity}
\end{equation}
Hence dynamic compaction removes exactly
\begin{equation}
R=Wn_{\max}-\sum_{t=1}^{W}n_t
\label{eq:removable-updates}
\end{equation}
problem updates relative to a fixed-width batch with the same final
boundary.
\end{proposition}
\begin{proof}
Use $n_t=\sum_{k\geq1}\mathbf 1\{n_t\geq k\}$ and exchange the two finite
sums. Fixed-width execution keeps all $W$ lanes through $n_{\max}$.
\end{proof}

Logical masking changes which states update but not the width seen by
the kernel, so it retains $Wn_{\max}$ updates. The area between the static
rectangle and the active-width trajectory is therefore the exact work removed
by dynamic compaction. The verification interval is already reflected in the observed $n_t$;
the first passing iteration on a denser verification schedule would define a different
quantity.

\begin{proposition}[Condition for removable work]
Let $(n_1,\ldots,n_W)$ be any random vector of finite iteration numbers.
Then $\mathbb E[R]>0$ if and only if there exists a batched iteration $k$ such that
\begin{equation}
\Pr(0<A_k<W)>0.
\label{eq:joint-iteration-condition}
\end{equation}
\end{proposition}
\begin{proof}
$R$ is nonnegative and is zero exactly when every lane has the same iteration
number. Different finite iteration numbers create a value of $k$ with both
active and completed lanes, and the converse follows directly from the
definition of $A_k$.
\end{proof}

The condition depends on the joint distribution of iteration numbers inside a
packed window. Perfectly synchronized lanes create no removable work even when
the required iteration numbers vary across windows.

\subsection{Exact wall-clock accounting for a measured backend}

Let $c_k(a,\xi_k)$ be the cost of matched update $k$ at width $a$,
where $\xi_k$ records support shape, data type, kernel configuration, and other
backend state held fixed by the comparison. Then
\begin{equation}
\begin{split}
T_{\mathrm{active}}={}&\sum_k c_k(A_k,\xi_k)
+H_{\mathrm{screen}}+H_{\mathrm{verification}}\\
&+H_{\mathrm{compact}}+H_{\mathrm{other}},
\end{split}
\label{eq:time-accounting}
\end{equation}
$H_{\mathrm{other}}$ covers remaining timed control work. Let $H_{\mathrm{fixed}}$
be the corresponding fixed-width control cost and let $\Delta H$ denote active
control cost minus $H_{\mathrm{fixed}}$.

\begin{proposition}[Runtime decomposition]
For a matched fixed-width/dynamic-compaction execution with the same final verifier
boundary and matched update states $\xi_k$,
\begin{equation}
T_{\mathrm{fixed}}-T_{\mathrm{active}}
=\sum_{k=1}^{n_{\max}}
\left[c_k(W,\xi_k)-c_k(A_k,\xi_k)\right]-\Delta H.
\label{eq:time-difference}
\end{equation}
Consequently, dynamic compaction is faster exactly when
\begin{equation}
\sum_k\!\left[c_k(W,\xi_k)-c_k(A_k,\xi_k)\right]
>\Delta H.
\label{eq:active-gate}
\end{equation}
\end{proposition}
\begin{proof}
Write $T_{\mathrm{fixed}}=\sum_k c_k(W,\xi_k)+H_{\mathrm{fixed}}$,
subtract Equation~\eqref{eq:time-accounting}, and collect the control terms.
\end{proof}

Variation among $n_t$ creates removable updates. The measured function
$c_k(a,\xi_k)$ determines whether the smaller active widths reduce kernel time.
If $c_k(A_k,\xi_k)=c_k(W,\xi_k)$ for every $k$, compaction removes updates but
saves no update time. It reduces total runtime when the summed update-time
reduction exceeds $\Delta H$.

\subsection{Width-dependent kernel time}

For the packed Triton update used in our implementation, the launch grid has
\begin{equation}
B(a,n)=a\left\lceil\frac{n}{b_m}\right\rceil
\label{eq:block-grid}
\end{equation}
thread blocks, where $b_m$ is the row tile size. GPU scheduling processes this
grid in occupancy-dependent waves \citep{nvidia2022gpuperformance,nvidia2024cuda}. The resulting model is
\begin{equation}
c(a;n,\xi)\approx g_{n,\xi}\!\left(
\left\lceil\frac{B(a,n)}{Q_{\xi}}\right\rceil\right),
\label{eq:wave-cost}
\end{equation}
where $Q_{\xi}$ is the measured resident block capacity of the kernel and
$g_{n,\xi}$ maps wave count to time. Registers, shared memory, and kernel state
enter through $Q_{\xi}$; the value is calibrated for the selected device and
kernel configuration.

On the measured A100/Triton configuration with $b_m=64$, the first-wave estimate
places the saturation width near $6.75$ for $n=1024$, $1.69$ for $n=4096$, and
below one for $n=16384$. Direct primitive measurements locate the first large
increase at widths $6\rightarrow7$ and $1\rightarrow2$ for the first two sizes;
the $n=16384$ curve is responsive from width one. These measurements instantiate
$c(a)$ for this backend and explain why equal update-count reductions can have different
wall-clock value across problem sizes.
